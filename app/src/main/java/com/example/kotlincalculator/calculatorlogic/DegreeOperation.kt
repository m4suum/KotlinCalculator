package com.example.kotlincalculator.calculatorlogic

import kotlin.math.pow
class DegreeOperation {
    fun degree(firstNumber: Double, secondNumber: Double) = firstNumber.pow(secondNumber).toString()
}